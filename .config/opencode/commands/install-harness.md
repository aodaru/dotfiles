---
description: Instala el arnés de agentes en el proyecto actual
---

Ejecuta el instalador de harness:

1. El script está en `~/.config/opencode/commands/harness_script/install.py`
2. Corre: `python3 ~/.config/opencode/commands/harness_script/install.py $ARGUMENTS`
3. El target es el directorio actual del proyecto
4. El script ya maneja auto-detect, preview, confirmación y rollback
